// Production: point to the Render API web service URL.
// Replace the placeholder with your actual Render API service URL after first deploy.
export const environment = {
  production: true,
  apiUrl: 'https://innovat-bharat-api.onrender.com',
};
